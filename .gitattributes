# 부서 전원이 Windows이므로 체크아웃은 CRLF로 통일한다.
* text=auto eol=crlf

# 바이너리는 줄바꿈 변환 금지 (변환하면 파일이 깨진다)
*.png  binary
*.jpg  binary
*.pdf  binary
*.xlsx binary
*.xlsm binary
*.docx binary
*.hwp  binary
*.parquet binary
